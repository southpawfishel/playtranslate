<?xml version="1.0" encoding="utf-8"?>
<!-- Bunpro settings sub-page. Structure follows activity_llm_backend_settings
     (key field + save-with-validation), plus an enable switch: unlike the LLM
     backends, whose "enabled" is derived from key presence, Bunpro keeps an
     independent toggle so the feature can be silenced without discarding a
     working token. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="?attr/ptSurface"
        app:navigationIcon="@drawable/ic_close"
        app:navigationIconTint="?attr/ptText"
        app:navigationContentDescription="@string/bunpro_settings_close_cd"
        app:title="@string/bunpro_settings_title"
        app:titleTextColor="?attr/ptText" />

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="?attr/ptDivider" />

    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:fillViewport="true">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <com.google.android.material.card.MaterialCardView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                app:cardBackgroundColor="?attr/ptCard"
                app:cardCornerRadius="@dimen/pt_radius"
                app:cardElevation="0dp"
                app:strokeColor="?attr/ptDivider"
                app:strokeWidth="1dp">

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical">

                    <LinearLayout
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:orientation="vertical"
                        android:paddingHorizontal="@dimen/pt_row_h_padding"
                        android:paddingVertical="@dimen/pt_row_v_padding">

                        <TextView
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:text="@string/bunpro_token_field_label"
                            style="@style/Text.PT.RowTitle" />

                        <EditText
                            android:id="@+id/etBunproToken"
                            android:layout_width="match_parent"
                            android:layout_height="wrap_content"
                            android:layout_marginTop="8dp"
                            android:hint="@string/hint_bunpro_token"
                            android:textColorHint="?attr/ptTextHint"
                            android:textColor="?attr/ptText"
                            android:textSize="13sp"
                            android:fontFamily="monospace"
                            android:singleLine="true"
                            android:inputType="textVisiblePassword"
                            android:background="?attr/ptSurface"
                            android:padding="10dp"
                            android:importantForAutofill="no" />
                    </LinearLayout>

                    <include layout="@layout/settings_row_divider" />

                    <include layout="@layout/settings_row_switch"
                        android:id="@+id/rowBunproEnabled" />

                    <TextView
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:background="@drawable/bg_word_tatoeba_attribution"
                        android:paddingHorizontal="16dp"
                        android:paddingVertical="10dp"
                        android:text="@string/bunpro_settings_about"
                        android:textColor="?attr/ptTextHint"
                        android:textSize="11sp" />

                </LinearLayout>
            </com.google.android.material.card.MaterialCardView>

        </LinearLayout>
    </androidx.core.widget.NestedScrollView>

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="?attr/ptDivider" />

    <!-- FrameLayout so progressSave can overlay the button during the
         validation ping (see LlmBackendSettingsActivity.setLoading). -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:layout_marginHorizontal="16dp"
        android:layout_marginVertical="8dp">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnSave"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:text="@string/deepl_settings_save"
            android:textAllCaps="false"
            android:textSize="15sp"
            app:cornerRadius="12dp" />

        <ProgressBar
            android:id="@+id/progressSave"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_gravity="center"
            android:indeterminate="true"
            android:visibility="gone"
            android:indeterminateTint="?attr/colorOnPrimary" />
    </FrameLayout>

</LinearLayout>
